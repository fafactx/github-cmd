 git init
 git add *
 git commit -am "First Commit T2080rdb uboot"
 git remote add origin https://github.com/fafactx/uboot-t2080.git
 git remote rm origin
 git pull
 git push -u -f origin master
 git clone https://github.com/fafactx/uboot-t2080.git
 git status
 git add README 
 git push origin master
 git status
 git commit -m "update by liguofa for Nor flash make cmd"
 git push origin master
